# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Production Network Operator workflow. It invokes the real l8k and kubectl
# executables inherited from provider.yaml. Test doubles live only under tests/.
#
# Usage:
#   ISVTEST_INCLUDE_UNRELEASED=1 uv run isvctl test run \
#     -f isvctl/configs/providers/k8s-launch-kit/config/network-operator.yaml \
#     --capability kubernetes --no-upload -- -v

import:
  - provider.yaml
  - ../../../suites/k8s-launch-kit/network-operator-use-cases.yaml

version: "1.0"

context:
  k8s_launch_kit:
    executable: l8k
    installation:
      mode: verify
      version: ""
      prefix: ""
    # Optional complete Launch Kit configuration. Every selected use case
    # stages its own copy before discovery; the source file is never modified.
    user_config: ""
    # An empty override means the adapter invokes kubectl from PATH. Users may
    # replace this with any kubectl-compatible argv list in an overlay.
    kubectl_command: []
    environment: {}
    shared_artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/shared-evidence
    use_cases:
      roce_sriov:
        working_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/roce-sriov/work
        artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/roce-sriov/evidence
        discover:
          arguments:
            - --fabric
            - ethernet
            - --deployment-type
            - sriov
        generate:
          arguments: []
        deploy:
          arguments: []
        validate:
          arguments: []
        clean:
          arguments: []
      infiniband_sriov:
        working_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/infiniband-sriov/work
        artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/infiniband-sriov/evidence
        discover:
          arguments:
            - --fabric
            - infiniband
            - --deployment-type
            - sriov
        generate:
          arguments: []
        deploy:
          arguments: []
        validate:
          arguments: []
        clean:
          arguments: []
      roce_rdma_shared:
        working_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/roce-rdma-shared/work
        artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/roce-rdma-shared/evidence
        discover:
          arguments:
            - --fabric
            - ethernet
            - --deployment-type
            - rdma_shared
        generate:
          arguments: []
        deploy:
          arguments: []
        validate:
          arguments: []
        clean:
          arguments: []
      infiniband_rdma_shared:
        working_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/infiniband-rdma-shared/work
        artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/infiniband-rdma-shared/evidence
        discover:
          arguments:
            - --fabric
            - infiniband
            - --deployment-type
            - rdma_shared
        generate:
          arguments: []
        deploy:
          arguments: []
        validate:
          arguments: []
        clean:
          arguments: []
      roce_host_device:
        working_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/roce-host-device/work
        artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/roce-host-device/evidence
        discover:
          arguments:
            - --fabric
            - ethernet
            - --deployment-type
            - host_device
        generate:
          arguments: []
        deploy:
          arguments: []
        validate:
          arguments: []
        clean:
          arguments: []
      infiniband_host_device:
        working_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/infiniband-host-device/work
        artifact_dir: ../../../../../_output/k8s-launch-kit/network-operator/use-cases/infiniband-host-device/evidence
        discover:
          arguments:
            - --fabric
            - infiniband
            - --deployment-type
            - host_device
        generate:
          arguments: []
        deploy:
          arguments: []
        validate:
          arguments: []
        clean:
          arguments: []

commands:
  network_operator:
    phases:
      - setup
      - launch-kit-verification
      - roce-sriov
      - infiniband-sriov
      - roce-rdma-shared
      - infiniband-rdma-shared
      - roce-host-device
      - infiniband-host-device
      - teardown
    continue_after_failure:
      - roce-sriov
      - infiniband-sriov
      - roce-rdma-shared
      - infiniband-rdma-shared
      - roce-host-device
      - infiniband-host-device
    steps:
      - name: launch_kit_prepare
        phase: setup
        command: python3 ../scripts/adapter.py
        args:
          - prepare
          - --mode
          - "{{ context.k8s_launch_kit.installation.mode }}"
          - --executable
          - "{{ context.k8s_launch_kit.executable }}"
          - "--version={{ context.k8s_launch_kit.installation.version }}"
          - "--prefix={{ context.k8s_launch_kit.installation.prefix }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.shared_artifact_dir }}"
        timeout: 900
        output_schema: k8s_launch_kit
        requires: [kubernetes]

      - name: launch_kit_verify
        phase: launch-kit-verification
        command: python3 ../scripts/adapter.py
        args:
          - verify
          - --executable
          - "{{ steps.launch_kit_prepare.executable | default(context.k8s_launch_kit.executable) }}"
          - "--expected-version={{ context.k8s_launch_kit.installation.version }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.shared_artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]

      # roce-sriov: preflight -> discover -> generate -> deploy -> validate -> clean
      - name: launch_kit_roce_sriov_preflight
        phase: roce-sriov
        command: python3 ../scripts/adapter.py
        args:
          - preflight
          - --kubectl-command-json
          - "{{ context.k8s_launch_kit.kubectl_command | tojson }}"
          - --workflow-arguments-json
          - "{{ {'discover': context.k8s_launch_kit.use_cases.roce_sriov.discover.arguments, 'generate': context.k8s_launch_kit.use_cases.roce_sriov.generate.arguments, 'deploy': context.k8s_launch_kit.use_cases.roce_sriov.deploy.arguments, 'validate': context.k8s_launch_kit.use_cases.roce_sriov.validate.arguments, 'clean': context.k8s_launch_kit.use_cases.roce_sriov.clean.arguments} | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceSriovCheck]

      - name: launch_kit_roce_sriov_discover
        phase: roce-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - discover
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.discover.arguments | tojson }}"
          - "--user-config={{ context.k8s_launch_kit.user_config }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.artifact_dir }}"
        timeout: 1800
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceSriovCheck]

      - name: launch_kit_roce_sriov_generate
        phase: roce-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - generate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.generate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.artifact_dir }}"
        timeout: 600
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceSriovCheck]

      - name: launch_kit_roce_sriov_deploy
        phase: roce-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - deploy
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.deploy.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceSriovCheck]

      - name: launch_kit_roce_sriov_validate
        phase: roce-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - validate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.validate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.artifact_dir }}"
        timeout: null
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceSriovCheck]

      - name: launch_kit_roce_sriov_clean
        phase: teardown
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable | default(context.k8s_launch_kit.executable) }}"
          - --command
          - clean
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.clean.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_sriov.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceSriovCheck]
        finalizer_for: launch_kit_roce_sriov_deploy

      # infiniband-sriov: preflight -> discover -> generate -> deploy -> validate -> clean
      - name: launch_kit_infiniband_sriov_preflight
        phase: infiniband-sriov
        command: python3 ../scripts/adapter.py
        args:
          - preflight
          - --kubectl-command-json
          - "{{ context.k8s_launch_kit.kubectl_command | tojson }}"
          - --workflow-arguments-json
          - "{{ {'discover': context.k8s_launch_kit.use_cases.infiniband_sriov.discover.arguments, 'generate': context.k8s_launch_kit.use_cases.infiniband_sriov.generate.arguments, 'deploy': context.k8s_launch_kit.use_cases.infiniband_sriov.deploy.arguments, 'validate': context.k8s_launch_kit.use_cases.infiniband_sriov.validate.arguments, 'clean': context.k8s_launch_kit.use_cases.infiniband_sriov.clean.arguments} | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandSriovCheck]

      - name: launch_kit_infiniband_sriov_discover
        phase: infiniband-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - discover
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.discover.arguments | tojson }}"
          - "--user-config={{ context.k8s_launch_kit.user_config }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.artifact_dir }}"
        timeout: 1800
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandSriovCheck]

      - name: launch_kit_infiniband_sriov_generate
        phase: infiniband-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - generate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.generate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.artifact_dir }}"
        timeout: 600
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandSriovCheck]

      - name: launch_kit_infiniband_sriov_deploy
        phase: infiniband-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - deploy
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.deploy.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandSriovCheck]

      - name: launch_kit_infiniband_sriov_validate
        phase: infiniband-sriov
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - validate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.validate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.artifact_dir }}"
        timeout: null
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandSriovCheck]

      - name: launch_kit_infiniband_sriov_clean
        phase: teardown
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable | default(context.k8s_launch_kit.executable) }}"
          - --command
          - clean
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.clean.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_sriov.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandSriovCheck]
        finalizer_for: launch_kit_infiniband_sriov_deploy

      # roce-rdma-shared: preflight -> discover -> generate -> deploy -> validate -> clean
      - name: launch_kit_roce_rdma_shared_preflight
        phase: roce-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - preflight
          - --kubectl-command-json
          - "{{ context.k8s_launch_kit.kubectl_command | tojson }}"
          - --workflow-arguments-json
          - "{{ {'discover': context.k8s_launch_kit.use_cases.roce_rdma_shared.discover.arguments, 'generate': context.k8s_launch_kit.use_cases.roce_rdma_shared.generate.arguments, 'deploy': context.k8s_launch_kit.use_cases.roce_rdma_shared.deploy.arguments, 'validate': context.k8s_launch_kit.use_cases.roce_rdma_shared.validate.arguments, 'clean': context.k8s_launch_kit.use_cases.roce_rdma_shared.clean.arguments} | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceRdmaSharedCheck]

      - name: launch_kit_roce_rdma_shared_discover
        phase: roce-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - discover
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.discover.arguments | tojson }}"
          - "--user-config={{ context.k8s_launch_kit.user_config }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.artifact_dir }}"
        timeout: 1800
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceRdmaSharedCheck]

      - name: launch_kit_roce_rdma_shared_generate
        phase: roce-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - generate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.generate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.artifact_dir }}"
        timeout: 600
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceRdmaSharedCheck]

      - name: launch_kit_roce_rdma_shared_deploy
        phase: roce-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - deploy
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.deploy.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceRdmaSharedCheck]

      - name: launch_kit_roce_rdma_shared_validate
        phase: roce-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - validate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.validate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.artifact_dir }}"
        timeout: null
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceRdmaSharedCheck]

      - name: launch_kit_roce_rdma_shared_clean
        phase: teardown
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable | default(context.k8s_launch_kit.executable) }}"
          - --command
          - clean
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.clean.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_rdma_shared.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceRdmaSharedCheck]
        finalizer_for: launch_kit_roce_rdma_shared_deploy

      # infiniband-rdma-shared: preflight -> discover -> generate -> deploy -> validate -> clean
      - name: launch_kit_infiniband_rdma_shared_preflight
        phase: infiniband-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - preflight
          - --kubectl-command-json
          - "{{ context.k8s_launch_kit.kubectl_command | tojson }}"
          - --workflow-arguments-json
          - "{{ {'discover': context.k8s_launch_kit.use_cases.infiniband_rdma_shared.discover.arguments, 'generate': context.k8s_launch_kit.use_cases.infiniband_rdma_shared.generate.arguments, 'deploy': context.k8s_launch_kit.use_cases.infiniband_rdma_shared.deploy.arguments, 'validate': context.k8s_launch_kit.use_cases.infiniband_rdma_shared.validate.arguments, 'clean': context.k8s_launch_kit.use_cases.infiniband_rdma_shared.clean.arguments} | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandRdmaSharedCheck]

      - name: launch_kit_infiniband_rdma_shared_discover
        phase: infiniband-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - discover
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.discover.arguments | tojson }}"
          - "--user-config={{ context.k8s_launch_kit.user_config }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.artifact_dir }}"
        timeout: 1800
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandRdmaSharedCheck]

      - name: launch_kit_infiniband_rdma_shared_generate
        phase: infiniband-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - generate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.generate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.artifact_dir }}"
        timeout: 600
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandRdmaSharedCheck]

      - name: launch_kit_infiniband_rdma_shared_deploy
        phase: infiniband-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - deploy
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.deploy.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandRdmaSharedCheck]

      - name: launch_kit_infiniband_rdma_shared_validate
        phase: infiniband-rdma-shared
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - validate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.validate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.artifact_dir }}"
        timeout: null
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandRdmaSharedCheck]

      - name: launch_kit_infiniband_rdma_shared_clean
        phase: teardown
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable | default(context.k8s_launch_kit.executable) }}"
          - --command
          - clean
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.clean.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_rdma_shared.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandRdmaSharedCheck]
        finalizer_for: launch_kit_infiniband_rdma_shared_deploy

      # roce-host-device: preflight -> discover -> generate -> deploy -> validate -> clean
      - name: launch_kit_roce_host_device_preflight
        phase: roce-host-device
        command: python3 ../scripts/adapter.py
        args:
          - preflight
          - --kubectl-command-json
          - "{{ context.k8s_launch_kit.kubectl_command | tojson }}"
          - --workflow-arguments-json
          - "{{ {'discover': context.k8s_launch_kit.use_cases.roce_host_device.discover.arguments, 'generate': context.k8s_launch_kit.use_cases.roce_host_device.generate.arguments, 'deploy': context.k8s_launch_kit.use_cases.roce_host_device.deploy.arguments, 'validate': context.k8s_launch_kit.use_cases.roce_host_device.validate.arguments, 'clean': context.k8s_launch_kit.use_cases.roce_host_device.clean.arguments} | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceHostDeviceCheck]

      - name: launch_kit_roce_host_device_discover
        phase: roce-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - discover
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.discover.arguments | tojson }}"
          - "--user-config={{ context.k8s_launch_kit.user_config }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.artifact_dir }}"
        timeout: 1800
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceHostDeviceCheck]

      - name: launch_kit_roce_host_device_generate
        phase: roce-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - generate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.generate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.artifact_dir }}"
        timeout: 600
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceHostDeviceCheck]

      - name: launch_kit_roce_host_device_deploy
        phase: roce-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - deploy
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.deploy.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceHostDeviceCheck]

      - name: launch_kit_roce_host_device_validate
        phase: roce-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - validate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.validate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.artifact_dir }}"
        timeout: null
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceHostDeviceCheck]

      - name: launch_kit_roce_host_device_clean
        phase: teardown
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable | default(context.k8s_launch_kit.executable) }}"
          - --command
          - clean
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.clean.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.roce_host_device.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkRoceHostDeviceCheck]
        finalizer_for: launch_kit_roce_host_device_deploy

      # infiniband-host-device: preflight -> discover -> generate -> deploy -> validate -> clean
      - name: launch_kit_infiniband_host_device_preflight
        phase: infiniband-host-device
        command: python3 ../scripts/adapter.py
        args:
          - preflight
          - --kubectl-command-json
          - "{{ context.k8s_launch_kit.kubectl_command | tojson }}"
          - --workflow-arguments-json
          - "{{ {'discover': context.k8s_launch_kit.use_cases.infiniband_host_device.discover.arguments, 'generate': context.k8s_launch_kit.use_cases.infiniband_host_device.generate.arguments, 'deploy': context.k8s_launch_kit.use_cases.infiniband_host_device.deploy.arguments, 'validate': context.k8s_launch_kit.use_cases.infiniband_host_device.validate.arguments, 'clean': context.k8s_launch_kit.use_cases.infiniband_host_device.clean.arguments} | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.artifact_dir }}"
        timeout: 60
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandHostDeviceCheck]

      - name: launch_kit_infiniband_host_device_discover
        phase: infiniband-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - discover
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.discover.arguments | tojson }}"
          - "--user-config={{ context.k8s_launch_kit.user_config }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.artifact_dir }}"
        timeout: 1800
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandHostDeviceCheck]

      - name: launch_kit_infiniband_host_device_generate
        phase: infiniband-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - generate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.generate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.artifact_dir }}"
        timeout: 600
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandHostDeviceCheck]

      - name: launch_kit_infiniband_host_device_deploy
        phase: infiniband-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - deploy
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.deploy.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandHostDeviceCheck]

      - name: launch_kit_infiniband_host_device_validate
        phase: infiniband-host-device
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable }}"
          - --command
          - validate
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.validate.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.artifact_dir }}"
        timeout: null
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandHostDeviceCheck]

      - name: launch_kit_infiniband_host_device_clean
        phase: teardown
        command: python3 ../scripts/adapter.py
        args:
          - run
          - --executable
          - "{{ steps.launch_kit_verify.executable | default(context.k8s_launch_kit.executable) }}"
          - --command
          - clean
          - --arguments-json
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.clean.arguments | tojson }}"
          - --environment-json
          - "{{ context.k8s_launch_kit.environment | tojson }}"
          - --working-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.working_dir }}"
          - --artifact-dir
          - "{{ context.k8s_launch_kit.use_cases.infiniband_host_device.artifact_dir }}"
        timeout: 7200
        output_schema: k8s_launch_kit
        requires: [kubernetes]
        requires_selected_validations: [EastWestNetworkInfiniBandHostDeviceCheck]
        finalizer_for: launch_kit_infiniband_host_device_deploy
